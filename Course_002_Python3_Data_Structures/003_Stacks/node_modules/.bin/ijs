#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../ijavascript/bin/ijavascript.js" "$@"
else 
  exec node  "$basedir/../ijavascript/bin/ijavascript.js" "$@"
fi
